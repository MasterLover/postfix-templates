## js  ts  operations ##

.llog : Inserts a console.log  with linenumbercall.
	ANY  →  console.log("Line:[$LINE*:lineNumber():""$] \t ", $expr$)


.tout : set timeout.
	ANY  →  setTimeout(() => { \
    			$END$ \
  			}, $expr$)


.reactive : to reactive.
	ANY  →  reactive($expr$)
